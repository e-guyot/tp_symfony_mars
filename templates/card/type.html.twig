{% extends 'base.html.twig' %}

{% block title %}Register a type{% endblock %}

{% block body %}
<h1>Register a Type</h1>
<div class="container">

    {{ form_start(form) }}
    {{ form_row(form.name) }}

    <button class="btn  btn-primary">Register</button>
    {{ form_end(form) }}
</div>
{% endblock %}